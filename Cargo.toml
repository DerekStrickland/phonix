[package]
name = "phonix"
version = "0.1.0"
authors = ["derekstrickland"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
vst = "0.2.1"
rand = "0.3"

[lib]
name = "delayvst"
crate-type = ["cdylib"]